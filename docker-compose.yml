version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nr1-web
    ports:
      - "80:80"
    restart: unless-stopped

networks:
  default:
    name: app-net
    external: true
